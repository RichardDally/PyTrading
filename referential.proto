syntax = "proto3";

message Referential
{
   message Instrument
   {
     uint64 identifier          = 1;
     string name                = 2;
     string isin                = 3;
     uint64 currency_identifier = 4;
   }

  repeated Instrument instruments = 1;
}
